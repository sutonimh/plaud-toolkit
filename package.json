{
  "name": "plaud",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build:plugin": "npm run build -w @plaud/obsidian",
    "install:plugin": "bash scripts/install-plugin.sh"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^3.0.0",
    "tsx": "^4.7.0"
  }
}
